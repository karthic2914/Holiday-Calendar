import { EncodingValue } from './interfaces/EncodingValue';
export declare class CursorDataView {
    dataview: DataView;
    index: number;
    constructor(input: ArrayBuffer);
    read(): number;
    readString(length: number, encoding?: EncodingValue): string;
}
