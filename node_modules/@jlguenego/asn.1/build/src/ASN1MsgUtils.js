"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ASN1MsgUtils = void 0;
class ASN1MsgUtils {
    static query(msg, key, value) {
        if (msg[key] === value) {
            return msg;
        }
        if (msg.value instanceof Array) {
            for (const v of msg.value) {
                const result = ASN1MsgUtils.query(v, key, value);
                if (result !== null) {
                    return result;
                }
            }
        }
        return null;
    }
    static queryAll(msg, key, value) {
        const result = [];
        if (msg[key] === value) {
            result.push(msg);
            return result;
        }
        if (msg.value instanceof Array) {
            for (const v of msg.value) {
                const subResult = ASN1MsgUtils.queryAll(v, key, value);
                result.push(...subResult);
            }
        }
        return result;
    }
}
exports.ASN1MsgUtils = ASN1MsgUtils;
//# sourceMappingURL=ASN1MsgUtils.js.map