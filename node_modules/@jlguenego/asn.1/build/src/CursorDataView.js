"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.CursorDataView = void 0;
class CursorDataView {
    constructor(input) {
        this.index = 0;
        this.dataview = new DataView(input);
    }
    read() {
        const uint = this.dataview.getUint8(this.index);
        this.index += 1;
        return uint;
    }
    readString(length, encoding = 'hex') {
        const buffer = this.dataview.buffer.slice(this.index, this.index + length);
        const value = Buffer.from(buffer).toString(encoding);
        this.index += length;
        return value;
    }
}
exports.CursorDataView = CursorDataView;
//# sourceMappingURL=CursorDataView.js.map