import assert from 'assert';
import { NumberToken, TypeReference } from '../src/analysis/ASN1Lexer';
describe('Regex Test', () => {
    it('TypeReference', () => {
        var _a;
        const r = TypeReference.PATTERN;
        assert('A-W'.match(r));
        assert(((_a = 'A--W'.match(r)) === null || _a === void 0 ? void 0 : _a[0]) === 'A');
        assert('asdf'.match(r) === null);
    });
    it('NumberToken', () => {
        const r = NumberToken.PATTERN;
        assert('123'.match(r));
        assert('0'.match(r));
        assert('asdf'.match(r) === null);
    });
});
//# sourceMappingURL=regex.spec.js.map