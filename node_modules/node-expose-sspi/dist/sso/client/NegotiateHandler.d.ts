import { RequestInit, Response } from 'node-fetch';
import { ClientCookie } from './ClientCookie';
import { ClientInfo } from './ClientInfo';
import { AbstractHandler } from './AbstractHandler';
export declare class NegotiateHandler extends AbstractHandler {
    private authenticationType;
    constructor(authenticationType?: string);
    handle(clientInfo: ClientInfo, clientCookie: ClientCookie, response: Response, resource: string, init?: RequestInit): Promise<Response>;
}
