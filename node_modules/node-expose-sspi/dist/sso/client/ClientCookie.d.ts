import { RequestInit, Response } from 'node-fetch';
export declare class ClientCookie {
    private cookieList;
    saveCookies(response: Response): void;
    restituteCookies(requestInit: RequestInit): void;
}
