import { CtxtHandle } from '../../lib/api';
import { User, SSOMethod, SSOObject, SSOOptions } from './interfaces';
export declare class SSO {
    private serverContextHandle;
    method: SSOMethod;
    user: User;
    owner: User;
    private options;
    constructor(serverContextHandle: CtxtHandle, method: SSOMethod);
    load(): Promise<void>;
    getJSON(): SSOObject;
    setOptions(options: Partial<SSOOptions>): void;
}
