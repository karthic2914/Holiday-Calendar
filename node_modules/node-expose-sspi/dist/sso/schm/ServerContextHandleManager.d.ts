import { IncomingMessage } from 'http';
import { CtxtHandle } from '../../../lib/api';
interface Item {
    timestamp: number;
    handle: CtxtHandle;
}
export declare class ServerContextHandleManager {
    cache: Map<string, Item>;
    release(req: IncomingMessage): void;
    get(req: IncomingMessage): CtxtHandle | undefined;
    set(req: IncomingMessage, handle: CtxtHandle): void;
    refresh(): void;
}
export {};
